/**
 * StatsCards Component
 * Compatibility wrapper for StatsCard
 */

import StatsCard from './StatsCard';

function StatsCards({ stats }) {
  return <StatsCard stats={stats} />;
}

export default StatsCards;
